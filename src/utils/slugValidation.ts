/**
 * Slug validation utility.
 *
 * Re-exports slug validation from slug.ts for consumers that import from this path.
 * The canonical implementation lives in slug.ts alongside generateSlug.
 */
export { validateSlug } from './slug';
